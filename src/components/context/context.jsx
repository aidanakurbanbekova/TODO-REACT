import React, from "react";
import ContentTheme from "../light dark";

// export const ThemeContent = React.createContext();

const ThemeContent = () => {
    return (
        <></>
    );
};

export default ThemeContent;
